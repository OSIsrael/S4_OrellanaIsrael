import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'SA4_OrellanaIsrael3E1',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
